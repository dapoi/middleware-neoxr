# Production Environment Variables for Zeabur
# Copy these to your Zeabur dashboard Environment Variables

NODE_ENV=production
SESSION_SECRET=sX6RHk4DZ2/LoTUR29Yvt+jPVa9/yrL3UpMb99Y/K/w=
API_KEY=4321lupa

# Redis Configuration for Zeabur
# Option 1: Leave these unset to use MemoryStore (simple but not ideal)
# Option 2: Add Redis service in Zeabur and set these variables:
# REDIS_HOST=${REDIS_HOST}           # Auto-injected by Zeabur Redis service
# REDIS_PORT=${REDIS_PORT}           # Auto-injected by Zeabur Redis service  
# REDIS_PASSWORD=${REDIS_PASSWORD}   # Auto-injected by Zeabur Redis service
# REDIS_DB=0                         # Set this manually

# Port (usually auto-assigned by Zeabur)
# PORT=8080

# Notes:
# - If REDIS_HOST is not set, the app will gracefully use MemoryStore
# - No more infinite error messages in production
# - To enable Redis: Add Redis service in Zeabur and set the variables above
